'use strict';
/* The timestamped backup/restore-point feature was removed. This test file is
   intentionally empty (the sandbox can't delete it). */
const test = require('node:test');
test('backups feature removed (no-op)', () => {});
