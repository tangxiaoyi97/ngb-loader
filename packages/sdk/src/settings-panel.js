// Intentionally empty. Neogebra does NOT ship a settings-UI template — plugin
// authors implement their own settings panel inside onOpenSettings(ctx). The
// framework only provides the entry point: a "Settings" button in the panel that
// calls the plugin's onOpenSettings hook. Kept as an empty module only because
// the sandbox can't delete it; nothing imports it.
export {};
